/** @type {import('next').NextConfig} */
// require('dotenv').config();
const nextConfig = {
    images: {
        domains: [
            'www.google.com'
        ]
    }
};

export default nextConfig;
